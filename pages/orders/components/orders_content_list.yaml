id: result_card
type: Card
style:
  marginTop: 4
blocks:
  # - id: title
  #   type: Title
  #   properties:
  #     content:
  #       _state: orders_list.length
  - id: orders_content_list
    type: List
    blocks:
      - id: orders_list.$
        type: Title
        properties:
          content: 
            _state: orders_list.$.order_type
      # - id: orders_list.$.box
      #   type: Box
      #   blocks:
      #     - id: orders_list.$.header_box
      #       type: Box
      #       layout:
      #         contentAlign: middle
      #         contentGutter: 16
      #       blocks:
      #         - id: orders_list.$.title
      #           type: Html
      #           layout:
      #             flex: 0 1 auto
      #           properties:
      #             html:
      #               _nunjucks:
      #                 template: |
      #                   <font style='font-size:12px' >
      #                     {{ order_type }}
      #                   </font>
      #                 on:
      #                   _state: orders_list.$
      #         - id: orders_list.$.id
      #           type: Html
      #           layout:
      #             flex: 1 1 auto
      #           properties:
      #             html:
      #               _nunjucks:
      #                 template: |
      #                   <font style='font-size:14px' >
      #                     <b>#{{ address }}</b>
      #                   </font>
      #                 on:
      #                   _state: orders_list.$
      #     - id: orders_list.$.date
      #       type: Html
      #       properties:
      #         html:
      #           _nunjucks:
      #             template: |
      #               <font style="font-size: 10px;" ><i>Created: {{ date_created | date("lll") }} {{ created_user.name }}</i></font>
      #             on:
      #               _state: orders_list.$
